@page "{unitNumber}"
@model arabella.Pages.Units.Children.IndexModel
@{
    ViewData["Title"] = "أولاد — أرابيلا";
    Layout = "_LayoutArabella";
}
<div class="container">
    <p class="text-muted">أولاد الوحدة @Model.UnitNumber — قريباً</p>
    <a asp-page="/Units/Index" asp-route-index="@Model.UnitIndex" class="btn btn-outline-secondary">العودة للوحدة</a>
</div>
