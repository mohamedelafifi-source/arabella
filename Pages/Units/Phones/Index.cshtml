@page "{unitNumber}"
@model arabella.Pages.Units.Phones.IndexModel
@{
    ViewData["Title"] = "هواتف — أرابيلا";
    Layout = "_LayoutArabella";
}
<div class="container">
    <p class="text-muted">هواتف الوحدة @Model.UnitNumber — قريباً</p>
    <a asp-page="/Units/Index" asp-route-index="@Model.UnitIndex" class="btn btn-outline-secondary">العودة للوحدة</a>
</div>
